#include<iostream>
#include<conio.h>
#include<math.h>
int main()
{
	int n=100,count=0;
	for(int i=0;i<=sqrt(n);i++)
	{
		if(n%i==0)
		{
			if(n/i==i)
			{
				printf("%d",i);
			}
			else
			{
				printf("%d",n/i);
				printf("%d",i);
			}
		count++;
		}
	}
}
